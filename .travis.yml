language: java
git:
  depth: 3
sudo: required
jdk:
  - oraclejdk7

before_script:
  - export MAVEN_OPTS='-Xms1g -Xmx2g'
  - export SEQWARE_SETTINGS="${TRAVIS_BUILD_DIR}/settings"
  - touch "${SEQWARE_SETTINGS}"
cache:
  directories:
    - $HOME/.m2
